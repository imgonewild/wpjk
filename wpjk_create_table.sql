CREATE TABLE wpjk (
    Plant varchar(255),
    Label varchar(255) NOT NULL,
    user varchar(255),
    location varchar(255),
    `IP address` varchar(255),
    `IP type` varchar(255),
    MAC varchar(255),
    `HostName` varchar(255),
    Brand varchar(255),
    Model varchar(255),
    `service ID/Serial number` varchar(255),
    CPU varchar(255),
    RAM varchar(255),
    Storage varchar(255),
    `OS type` varchar(255),
    `os product key` varchar(255),
    `Admin account` varchar(255),
    Password varchar(255),
    `office vision` varchar(255),
    `office product key` varchar(255),
    Old varchar(255),
    `Old product key` varchar(255),
    antivirus varchar(255),
    AS400 varchar(255),
    others varchar(255),
    UNIQUE (Label)
);